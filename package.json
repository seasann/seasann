{
  "name": "create-seasann-app",
  "version": "1.0.0-beta",
  "description": "Create a seasann app.",
  "main": "bundle.js",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint": "prettier --write .",
    "build-tsc": "tsc",
    "build": "rimraf build && pnpm lint && tsc && rollup --config && rimraf build"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "bin": ".bundle.js",
  "dependencies": {
    "inquirer": "^9.1.4",
    "node-async-exec": "^1.2.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.2",
    "@types/node": "^18.6.4",
    "husky": "^8.0.1",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "rollup": "^3.2.5",
    "typescript": "^4.7.4"
  }
}
